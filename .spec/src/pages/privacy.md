# 명세: src/pages/privacy.astro

## 역할 요약

개인정보처리방침 공개 페이지(`/privacy`, `PublicLayout`). 2026-09-20 초안 — `documents/privacy_redesign_plan.md` 1단계 완료 기준("로그인 이메일 미보관", "카카오는 이용 사실을 알게 됨")과 2단계 결과(실명·이메일·명부 미보관, 소속은 본인·관리자만, 비회원 공개 계층)를 사실대로 적는다. 시행일 `2026-09-20`·문의처(의견보내기·위원회, 이메일 미공개) **확정**(Dr. Ben 2026-09-20). 법적 검토로 문안이 바뀌면 새 시행일과 함께 갱신. 3단계(익명 제안) §8 포함.

## Props

없음. 시행일은 파일 상단 `EFFECTIVE_DATE`.

## 사이드 이펙트

없음(정적 텍스트). `auth-handler.ts` publicPaths에 `/privacy` 포함 — 비로그인 접근 가능.

## 핵심 규칙

1. **코드가 하는 일만 적는다** — 문서와 동작이 어긋나면 방침이 거짓이 된다. 데이터 모델이 바뀌면(컬럼 추가·삭제, 새 위탁 서비스) 같은 PR에서 이 페이지를 고친다.
2. 카카오: 닉네임 미요청(`login.astro` scope `account_email`만), 이메일은 계정 생성용으로 일시 전달 후 즉시 파생 주소로 교체 — 이 두 문장은 실제 코드(`claimUsername`, `performSelfHealing`)와 일치해야 한다.
3. 링크: 로그인 페이지 폼 아래(`.privacy-link`)와 사이드바 하단(`.footer-link`).
4. **사용 현황 집계(U 트랙) — 2026-09-20 도입, §3 에 현재형으로 기술.** 문장은 실제 구현과 1:1 로 대조해 유지한다: 쿠키·IP·외부 분석 없음(`src/lib/usage/record.ts`) · 날짜별 HMAC(`actorKeys`) · 시 단위 뭉개기(`truncateToHour`) · 원시 35일 파기(`USAGE_RETENTION_DAYS`) · 제도 개선 제안 화면 제외(`USAGE_EXCLUDED_PREFIXES`). **보존 일수를 환경변수로 바꾸면 이 문장도 같이 고쳐야 한다** — 방침이 실제와 어긋나면 안 된다.
5. 집계 도입은 이용자에게 알릴 변경(§10)이다. 배포와 같은 시점에 앱 내 알림으로 안내한다.

## 이력

- 2026-09-20: 초안 신설(1단계 완료 기준 마감) → 같은 날 §8 익명 제안 추가 · 시행일·문의처 확정.

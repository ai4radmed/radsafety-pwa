# 명세: src/actions/index.ts

## 역할 요약

Astro server actions 진입점. `defineAction`으로 `saveFinding`, `deleteFinding`, `sendVerificationCode`, `verifyEmailCode`, `sendNotification`, `sendFeedback`, `signUpWithUsername`, `signInWithUsername`, `claimUsername`, `approvePendingMember`, `rejectPendingMember`, `resolveHospitalRequest`를 export한다. Supabase, 이메일, 푸시, 로거를 사용한다.

## Public API (server 객체)

| 액션                                                                                            | input                                                                                                                  | 설명                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ----------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `saveFinding`                                                                                   | id?, title, findingType, tags, year, description, violationClause?, solution?                                          | findings 테이블 insert/update. `id`가 `local-`로 시작하면 insert                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `deleteFinding`                                                                                 | id                                                                                                                     | findings 삭제. `local-` id는 무시                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `sendNotification` (입력에 `noExpiry?` — true 면 `expires_at` NULL, 업데이트 안내용 2026-09-20) | senderId, targetType, provider?, verificationStatus?, specificUserId?, title, message, link?, actionLabel?, actionUrl? | 관리자만. profiles 조회 후 notifications insert, 웹 푸시 발송                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `sendFeedback`                                                                                  | userId?, userName, userEmail, title, message, attachments?                                                             | feedback insert, 관리자 이메일 발송                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `signUpWithUsername`                                                                            | username, password(8자+), hospitalId?, hospitalRequest?, society?                                                      | Stage 1-A + Phase 2 + 기관 등록 요청(규칙 14). `profiles.username` 중복확인 → `auth.admin.createUser`(가짜 이메일 `<username>@radsafety.invalid`, `email_confirm:true`) → `profiles` **upsert**(`onConflict: 'id'`, insert 아님 — 이유는 규칙 10), **`status: 'pending'`**·`hospital_id`·`society`·**`provider: 'email'`**(규칙 13) 포함(규칙 11). profiles upsert 실패 시 방금 만든 auth 계정 롤백(삭제). 반환 `{success, email}` — 클라이언트가 그 email 로 `signInWithPassword` 를 이어서 호출                                                |
| `signInWithUsername`                                                                            | username                                                                                                               | `profiles.username` → id → `auth.admin.getUserById` 로 email 조회만. 비밀번호 검증은 하지 않음(클라이언트가 반환된 email 로 `signInWithPassword` 호출). 아이디 없음/조회 실패 모두 같은 문구(`아이디 또는 비밀번호가 올바르지 않습니다.`)로 응답해 아이디 존재 여부 비노출                                                                                                                                                                                                                                                                       |
| `claimUsername`                                                                                 | userId(uuid), username, password?(8자+), hospitalId?, hospitalRequest?, society?                                       | Stage B(전환기간) + Phase 2 + 기관 등록 요청(규칙 14). 다른 사용자가 쓰는 아이디면 에러(본인 재확정은 허용). `auth.admin.updateUserById`로 email 을 가짜 이메일로 교체(password 주어지면 같이 설정) + `profiles.username/login_email(NULL)/nickname(NULL)` 갱신. `hospitalId`/`society`는 **필드가 아예 안 왔으면(undefined) 갱신 안 함** — 기존 active 계정의 평범한 전환(신규 필드 안 보임)에서 소속기관을 실수로 지우지 않기 위해(규칙 11). password 는 서버에서 optional — 강제 여부(카카오 선택/이메일 출신 사실상 필수)는 클라이언트 UI 몫 |
| `updateAffiliation`                                                                             | userId(uuid), hospitalId?, hospitalRequest?, society?(null 허용)                                                       | 2-2(2026-09-19) 마이페이지 소속 정보 저장(규칙 16). `assertKnownHospitalId` → `resolveHospitalFields` 로 `hospital_id`/`hospital_request` 산출 → profiles update(`society` 는 키가 왔을 때만, `null` 이면 지움). 요청 텍스트가 저장되면 관리자 전원 알림(`notifyAdminsOfHospitalRequest`). 반환 `{success, hospital_id, hospital_request, society}`                                                                                                                                                                                              |
| `reviewSubmission`                                                                              | adminId, kind('archive'/'finding'), id, decision('approve'/'reject'), reason?                                          | 2-1(2026-09-19) 첫 제출 검토(규칙 17). 관리자 확인 → 대상 행이 `pending`이어야 함. **approve**: archive 이고 `file_bucket='resources-pending'`이면 파일을 공개 버킷 `resources` 같은 경로로 복사 후 대기 파일 삭제(`movePendingFileToPublic`) → `status='published'`(+`file_bucket='resources'`) → 작성자 `can_publish=true` → 알림. **reject**: `status='rejected'` → 작성자 `reject_count+1` → 사유·누적 안내 알림(3회면 차단 문구). 반환 `{success, status, rejectCount?}`                                                                    |
| `setPublishPermission`                                                                          | adminId, targetUserId, canPublish(boolean)                                                                             | 2-1 관리자가 직접 게시 권한 부여/회수 → `profiles.can_publish` + 알림. 반환 `{success, canPublish}`                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `notifySubmission`                                                                              | kind('archive'/'finding'), id                                                                                          | 2-1 규칙 6(규칙 18). 제출 직후 제출자 클라이언트가 호출. 대상 행이 `pending`이면 관리자 전원(`is_admin`) in-app 알림(`createBulkNotifications`, 링크 `/admin/submissions`) + 텔레그램 1통(`sendTelegramMessage`). 둘 다 실패해도 throw 하지 않고 `{success, notified, telegram}`로 결과만 반환. pending 아니면 `{notified:false, telegram:false}`                                                                                                                                                                                                |
| `approvePendingMember`                                                                          | adminId, targetUserId                                                                                                  | Phase 3. 관리자 권한 확인 후 대상 계정 `profiles.status`를 `pending`→`active`로 변경 **+ `can_publish=true`**(2026-09-19, 가입 승인 = 게시 권한 — 2-1 축소)                                                                                                                                                                                                                                                                                                                                                                                      |
| `rejectPendingMember`                                                                           | adminId, targetUserId                                                                                                  | Phase 3. 관리자 권한 확인 후 대상 계정 `profiles.status`를 `banned`로 변경(규칙 12) — 재가입은 새 계정으로                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `resolveHospitalRequest`                                                                        | adminId, targetUserId, hospitalId?                                                                                     | 기관 등록 요청 처리 — **합치기/거절**(규칙 14·15). `'other'`는 즉시 거부. 관리자 권한 확인 후 `hospitalId`가 있으면 `isKnownHospitalId`(정적+`hospitals_custom`)로 검증해 `hospital_id` 갱신 + `hospital_request=NULL`(표기만 다른 기존 기관으로 합치기), 없으면 `hospital_request=NULL`만(거절, 소속 `기타` 유지). 어느 쪽이든 가입자에게 `system_notice` 알림 1건(실패해도 처리 결과는 유지). 반환 `{success, hospitalId(없으면 null)}`                                                                                                        |
| `registerHospitalFromRequest`                                                                   | adminId, targetUserId, name(2~60자)                                                                                    | 기관 등록 요청 처리 — **새 기관 등록**(규칙 15). 관리자 권한 확인 후 `findStaticHospitalByName(name)`에 걸리면 그 정적 기관으로 합침(`created:false`), 아니면 `customHospitalId(name)`로 `hospitals_custom` upsert(`onConflict:'id'`, `created_by=adminId`) 후 그 id로 `hospital_id` 갱신 + `hospital_request=NULL`, 가입자 알림 1건. 배포 없이 화면에서 끝난다. 반환 `{success, hospitalId, name, created}`                                                                                                                                     |

## 사이드 이펙트

- DB: findings, email_verification_codes, profiles, notifications, feedback
- Supabase Admin Auth API: `auth.admin.createUser`/`getUserById`/`updateUserById`/`deleteUser` (signUpWithUsername/signInWithUsername/claimUsername)
- 이메일: sendVerificationEmail, sendFeedbackEmail
- 푸시: sendPushToUsers
- 로거: createLogger('actions')

## 핵심 규칙

1. `accept: 'form'`은 saveFinding만 사용.
2. ~~sendVerificationCode~~ (2-2 삭제).
3. sendNotification: targetType별 profiles 필터(all/provider/specific — `verification_status` 대상은 2-2 에서 제거).
4. sendFeedback: 이메일 실패해도 DB 저장 성공 시 성공 처리.
5. sendFeedback 수신자: `resolveFeedbackRecipients(title, userEmail, DEVELOPER_EMAILS)` 로 결정. `DEVELOPER_EMAILS` 는 서버 전용 env(쉼표 구분, PUBLIC 아님)를 모듈 로드 시 파싱 — 테스트성 의견(`[월간점검]` + 관리자 발신)만 개발자 목록으로 라우팅되고, 미설정 시 종전대로 관리자 전원.
6. `usernameSchema`(모듈 내부): trim → 소문자화 → `^[a-z0-9_-]{3,20}$` 검증. 세 액션 공통 사용 — DB 에는 항상 소문자만 저장된다(대소문자 구분 없는 로그인은 저장 시 정규화로 구현, DB 단 citext/대소문자무시 인덱스는 쓰지 않음).
7. 가짜 이메일은 `<username>@radsafety.invalid` 로 결정적으로 파생(모듈 내부 `fakeEmailFor`) — RFC 2606 예약 도메인이라 실제 메일 발송·등록 불가.
8. **세션 쿠키는 이 세 액션이 직접 설정하지 않는다.** `context`(cookies/request)를 쓰지 않고 email 만 반환 — 기존 개발자 테스트 로그인(`login.astro`)과 동일하게 **클라이언트의 브라우저 supabase 클라이언트가 `signInWithPassword` 를 호출**해 세션을 연다. 이 코드베이스의 다른 액션들도 context 를 쓰지 않는 관례(테스트 모의 `tests/mocks/astro-actions.ts` 도 context 인자를 지원하지 않음)를 따른 것.
9. `claimUsername` 은 세션이 아니라 클라이언트가 넘긴 `userId` 를 그대로 신뢰한다 — `approveVerification`/`sendNotification` 등 기존 관리자 액션과 같은 관례(명시적 id 전달, context 기반 아님).
10. `signUpWithUsername`의 profiles 쓰기는 **반드시 `upsert(onConflict:'id')`** — 운영 Supabase 에 `auth.users` insert 시 `profiles` 에 빈 행을 자동 생성하는 DB 트리거가 있다(2026-09-16 프리뷰 실측: 새 아이디마다 재현되는 `duplicate key value violates unique constraint "profiles_pkey"` 로 발견, 어떤 username 을 써도 동일하게 재현돼 특정 값이 아닌 트리거 문제로 확정). 평범한 `insert` 로 되돌리면 가입이 항상 실패한다. 트리거 자체는 이 저장소의 `sql_query/*.sql` 에 없다(대시보드에서만 존재하는 것으로 추정) — 정확한 정의는 Supabase SQL Editor `select pg_get_triggerdef(oid) from pg_trigger where tgrelid = 'auth.users'::regclass;` 로 확인 가능.
11. **Phase 2(2계층+가입승인+제재 모델, KSNM 방안위 교육팀 합의 2026-09-10 승계)**: `hospitalIdSchema`(모듈 내부)는 빈 문자열을 `null`로 변환하고, 그 외 값은 `src/data/hospitals.ts`의 `HOSPITALS`에 실제로 존재하는 `id`인지 검증 — `HospitalAutocomplete.astro`가 목록 클릭으로만 값을 채우므로, 알 수 없는 id 가 오면(UI 우회 등) 조용히 버리지 않고 명확히 거부한다. `societySchema`는 `'nuclear_medicine' | 'technology' | 'none'` 이넘. 둘 다 선택 항목(`documents/privacy_redesign_plan.md` §2-2). `signUpWithUsername`은 신규 계정에 항상 `status: 'pending'`을 명시(관리자 승인 전까지 회원 계층 기능 접근 불가 — 접근 제한 자체는 Phase 4/5에서 구현). `claimUsername`은 `status`를 건드리지 않는다(자가치유로 막 생긴 pending 계정이든, 기존 active 계정이든 이미 정해진 값을 그대로 둔다).
12. **인증은 세션(2026-09-20 전환, `.spec/src/actions/auth.md`)**: 관리자 액션은 `requireAdmin(context)`, 회원 액션은 `requireUser(context)` — 클라이언트가 보내는 `adminId`/`userId`/`senderId` 는 optional 로 남아 있지만 **무시**된다(이전엔 클라이언트 값으로 `profiles.is_admin` 만 대조 — 관리자 UUID 만 알면 호출 가능했던 구조). Phase 3 거절은 `status: 'banned'`(CHECK 제약에 "가입 거절" 전용 값 없음), 봉인 계정 재개 절차 없음, 재가입은 새 username.
13. **`profiles.provider`(`sql_query/migrate_add_profile_provider.sql`, 2026-09-18)**: `signUpWithUsername`은 항상 `'email'`을 쓴다. Kakao 신규가입은 이 액션을 거치지 않고 `auth-handler.ts`의 자가 치유(`performSelfHealing`)가 대신 `'kakao'`를 쓴다 — 두 경로 모두 `.spec/src/lib/auth-handler.md` 규칙 4 참조. `member-approval.astro`가 이 컬럼을 select 하는데 DB에 없어 나던 "column profiles.provider does not exist" 로드 실패(2026-09-18 PR #52 프리뷰 실측)를 이 마이그레이션+코드 변경으로 수리.
14. **기관 등록 요청(`profiles.hospital_request`, `sql_query/migrate_add_hospital_request.sql`, 2026-09-19 — Dr. Ben)**: 가입자 편의 최우선 — 목록에 없는 기관은 별도 요청 절차 없이 **타이핑한 채로 그냥 진행**한다. `hospitalRequestSchema`(trim, 60자 이내, optional). `signUpWithUsername`/`claimUsername`은 모듈 내부 `resolveHospitalFields(hospitalId, hospitalRequest)`로 저장값을 정한다: `hospitalId`가 확정돼 있으면 `{hospital_id, hospital_request: null}`(요청 텍스트 무시), 없고 `hospitalRequest`가 있으면 `{hospital_id: 'other', hospital_request: <텍스트>}`, 둘 다 없으면 `{null, null}`. `claimUsername`은 `hospitalId`·`hospitalRequest` **둘 다 안 왔을 때만** 이 두 키를 건드리지 않는다(규칙 11의 "필드가 안 오면 갱신 안 함" 유지). 요청이 저장되면 `is_admin=true` 전원에게 `createBulkNotifications`(`system_notice`, 링크 `/admin/member-approval`) — 알림 실패는 삼키고 경고 로그만(가입을 막지 않는다). 처리는 `registerHospitalFromRequest`/`resolveHospitalRequest`(위 표).
15. **`hospitals_custom`(2026-09-19 개정 2, `sql_query/migrate_add_hospitals_custom.sql`)**: 관리자가 화면에서 배포 없이 등록한 기관. `hospitalIdSchema`는 더 이상 정적 목록만 보지 않는다 — 존재 확인이 DB를 포함해 비동기이므로 zod refine 대신 핸들러 첫 줄의 `assertKnownHospitalId`(`src/lib/hospitals.ts`의 `isKnownHospitalId`: 정적 → `c-` 접두일 때만 DB)로 한다. `signUpWithUsername`·`claimUsername`·`resolveHospitalRequest` 모두 이 검증을 거친다. 커스텀 id는 이름에서 결정적으로 도출(`c-`+sha256[:10]) — 같은 이름 재등록은 upsert로 흡수. 정적 목록과 표기만 다른 이름(`findStaticHospitalByName`)은 새로 만들지 않고 정적 기관으로 합친다. 공용 단계(`assertAdmin`·`readHospitalRequest`·`applyHospitalResolution`·`notifyHospitalResolution`)는 모듈 하단의 내부 함수.
16. **2-2(2026-09-19) — 개인정보 컬럼 삭제 이후**: `signUpWithUsername`·`claimUsername` 은 더 이상 `login_email`/`nickname` 을 쓰지 않는다(컬럼 자체가 없음, `sql_query/migrate_drop_legacy_profile_columns.sql`). `updateAffiliation` 은 마이페이지 카드 2 의 저장 액션으로, 가입 폼과 동일한 기관 처리 규칙(규칙 14·15)을 재사용한다 — 확정 id 검증, 미확정 텍스트는 `other`+요청+관리자 알림. 세션 대신 클라이언트가 넘긴 `userId` 를 신뢰하는 관례는 규칙 9 와 같다.
17. **2-1 업로드 권한(2026-09-19, `sql_query/migrate_add_publish_gate.sql`)**: 제출 행의 `status`는 DB 트리거가 정한다(클라이언트 값 무시) — 서버 액션은 서비스 롤이라 `auth.uid()`가 NULL이고 트리거가 손대지 않으므로 `reviewSubmission`이 직접 `published`/`rejected`를 쓴다. 승인 시 파일 이동은 **복사 성공 후 삭제** 순서(복사 실패 시 대기 파일 보존). 알림 실패는 처리 결과를 바꾸지 않는다(`notifySubmitter`가 삼킴). 반려 3회 누적의 실제 차단은 RLS INSERT 정책(`reject_count < 3`)이 한다 — 액션은 카운트만 올린다.
18. **첫 제출 관리자 알림(`notifySubmission`, 2026-09-19)**: 제출(insert)은 클라이언트가 Supabase에 직접 하므로 서버가 끼어들 지점이 없다 — 대신 클라이언트가 insert 응답의 `status==='pending'`이면 이 액션을 fire-and-forget 으로 호출한다. 서버는 행을 다시 읽어 pending 인지 확인한 뒤에만 알린다(클라이언트가 임의로 호출해도 게시된 행으로는 알림이 안 간다). 텔레그램은 `src/lib/telegram.ts`(Vercel env `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`, 미설정이면 생략). 알림 실패는 제출 결과에 영향 없음.

## reviewBulletin (K-1, 2026-09-20)

입력 `{ id, decision: 'publish'|'ignore'|'update', summary?, prepNote?, parentId?: uuid|null, checklistRefs? }`(`adminId` 는 무시). `requireAdmin(context)` 뒤 `bulletins` 행을 갱신한다.

- `publish`: `status='published'`, `published_at`, (summary/prepNote/parentId 있으면 함께) → **active 회원 전체** `createBulkNotifications(type system_notice, link /bulletins, 90일)`. `parentId`(또는 기존 parent) 가 있으면 제목 `후속: <제목>` + 본문에 상위 사건 제목, 없으면 `사건·사고: <제목>`. 이미 published 면 오류.
- `ignore`: `status='ignored'`. `update`: 필드만 저장(상태 유지).
- `checklistRefs?: string[]`(slug `^[a-z0-9-]+$`, ≤20) → `checklist_refs`(후속 2026-09-20).
- 거부: `parentId === id`(자기참조). 알림 실패는 로그만(게시는 완료).
- 화면: `src/pages/admin/bulletins.astro`. 테스트: `tests/unit/pages/bulletins.test.ts`.

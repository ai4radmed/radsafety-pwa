---
import DashboardLayout from '../layouts/DashboardLayout.astro';
import Icon from '../components/Icon.astro';
---

<DashboardLayout title="설정">
    <div class="settings-container">
        <h1><Icon name="settings" size="32" class="title-icon" /> 설정</h1>
        <p class="subtitle">앱의 화면 및 기능을 사용자 환경에 맞게 조정합니다.</p>

        <div class="setting-card">
            <div class="setting-header">
                <h2>화면 설정</h2>
                <p>글자 크기를 조절하여 더 편안하게 볼 수 있습니다.</p>
            </div>

            <div class="control-group">
                <div class="label">글자 크기 (Font Size)</div>
                <div class="font-preview" id="fontPreview">가가가 (미리보기)</div>
                <div class="font-control-options">
                    <button class="font-btn small" data-size="small" aria-label="글자 작게">
                        <span style="font-size: 0.8rem;">가</span>
                        <span class="btn-label">작게</span>
                    </button>
                    <button class="font-btn medium active" data-size="medium" aria-label="글자 보통">
                        <span style="font-size: 1rem;">가</span>
                        <span class="btn-label">보통</span>
                    </button>
                    <button class="font-btn large" data-size="large" aria-label="글자 크게">
                        <span style="font-size: 1.2rem;">가</span>
                        <span class="btn-label">크게</span>
                    </button>
                    <button class="font-btn xlarge" data-size="xlarge" aria-label="글자 아주 크게">
                        <span style="font-size: 1.5rem;">가</span>
                        <span class="btn-label">아주 크게</span>
                    </button>
                </div>
            </div>
        </div>

        <div class="setting-card" style="margin-top: 1.5rem; opacity: 0.7;">
            <div class="setting-header">
                <h2>알림 설정 (준비 중)</h2>
            </div>
            <div class="control-group">
                <div class="toggle-row">
                    <span>푸시 알림 받기</span>
                    <div class="toggle-switch disabled"></div>
                </div>
            </div>
        </div>
    </div>

    <style>
        .settings-container {
            max-width: 800px;
            margin: 0 auto;
        }
        h1 {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 0.5rem;
        }
        .subtitle {
            color: var(--text-muted);
            margin-bottom: 2rem;
            font-size: 1.1rem;
        }

        .setting-card {
            background: var(--bg-card);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            padding: 1.5rem;
            box-shadow: 0 1px 3px var(--shadow-color);
        }

        .setting-header h2 {
            font-size: 1.25rem;
            margin-bottom: 0.5rem;
            color: var(--text-main);
        }
        .setting-header p {
            color: var(--text-muted);
            font-size: 0.95rem;
            margin-bottom: 1.5rem;
        }

        .control-group {
            border-top: 1px solid var(--border-color);
            padding-top: 1.5rem;
        }

        .label {
            font-weight: 600;
            margin-bottom: 1rem;
            color: var(--accent);
        }

        .font-preview {
            background: var(--bg-body);
            padding: 1rem;
            border-radius: 8px;
            text-align: center;
            margin-bottom: 1rem;
            border: 1px solid var(--border-color);
            transition: font-size 0.2s;
        }

        .font-control-options {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 0.5rem;
        }

        .font-btn {
            background: var(--bg-body);
            border: 1px solid var(--border-color);
            color: var(--text-muted);
            padding: 1rem 0.5rem;
            border-radius: 8px;
            cursor: pointer;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.2s;
        }

        .font-btn:hover {
            border-color: var(--accent);
            color: var(--text-main);
        }

        .font-btn.active {
            background: rgba(37, 99, 235, 0.1);
            color: var(--accent);
            border-color: var(--accent);
            box-shadow: 0 0 0 2px rgba(37, 99, 235, 0.3);
        }

        .btn-label {
            font-size: 0.85rem !important; /* Fixed size label */
            opacity: 0.8;
        }

        /* Toggle Switch Mock */
        .toggle-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .toggle-switch {
            width: 40px;
            height: 24px;
            background: #4b5563;
            border-radius: 99px;
            position: relative;
        }
        .toggle-switch.disabled::after {
            content: '';
            position: absolute;
            left: 2px;
            top: 2px;
            width: 20px;
            height: 20px;
            background: #9ca3af;
            border-radius: 50%;
        }

        @media (max-width: 600px) {
            .font-control-options {
                grid-template-columns: repeat(2, 1fr);
            }
        }
    </style>

    <script>
        document.addEventListener('astro:page-load', () => {
            const fontBtns = document.querySelectorAll('.font-btn');
            const currentSize = localStorage.getItem('fontSize') || 'medium';

            // Initial Active State
            fontBtns.forEach((btn) => {
                if ((btn as HTMLElement).dataset.size === currentSize) {
                    btn.classList.add('active');
                } else {
                    btn.classList.remove('active');
                }

                btn.addEventListener('click', () => {
                    const newSize = (btn as HTMLElement).dataset.size!;

                    // 1. Update Global Font Size
                    document.documentElement.setAttribute('data-font-size', newSize);
                    localStorage.setItem('fontSize', newSize);

                    // 2. Update UI
                    fontBtns.forEach((b) => b.classList.remove('active'));
                    btn.classList.add('active');

                    // 3. Update Preview Text Size (Optional, as global changes handle it)
                    // But explicitly setting it for clarity if needed
                    // preview.style.fontSize = getComputedStyle(document.body).fontSize;
                });
            });
        });
    </script>
</DashboardLayout>

# PUBLIC_ADMIN_EMAILS=admin1@example.com,admin2@example.com
PUBLIC_ADMIN_EMAILS=admin@example.com

# Supabase Configuration
PUBLIC_SUPABASE_URL=https://your-project.supabase.co

PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Email Service (Resend)
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here
RESEND_FROM_EMAIL=noreply@yourdomain.com

# 개발자 이메일 (서버 전용, 쉼표 구분) — [월간점검] 접두어 + 관리자 발신 의견 메일의 수신자.
# 미설정 시 테스트 의견도 관리자 전원(PUBLIC_ADMIN_EMAILS)에게 발송된다.
# DEVELOPER_EMAILS=dev1@example.com,dev2@example.com

# Note: RESEND_API_KEY와 SUPABASE_SERVICE_ROLE_KEY는 서버 사이드에서만 사용됩니다 (PUBLIC_ 접두사 없음)
# SUPABASE_SERVICE_ROLE_KEY는 Supabase Dashboard > Settings > API > service_role key에서 확인 가능

# Web Push (VAPID) - 웹 푸시 알림
# 생성: node -e "const wp=require('web-push'); const k=wp.generateVAPIDKeys(); console.log(k);"
PUBLIC_VAPID_KEY=your-vapid-public-key-here
VAPID_PRIVATE_KEY=your-vapid-private-key-here
VAPID_EMAIL=mailto:noreply@yourdomain.com

# 개발용 테스트 계정 (Preview/로컬 전용 — Production에는 설정 금지)
# PUBLIC_DEV_MODE=true 일 때만 /login 페이지에 [개발자 모드] 버튼이 표시됩니다.
# PUBLIC_DEV_MODE=true
# DEV_TEST_USER_EMAIL=test-user@radsafety.kr
# DEV_TEST_USER_PASSWORD=your-test-user-password
# DEV_TEST_ADMIN_EMAIL=test-admin@radsafety.kr
# DEV_TEST_ADMIN_PASSWORD=your-test-admin-password

# Vercel Cron 인증 (src/pages/api/cron/watch.ts). Vercel 이 이 값을 Authorization: Bearer 로 붙여 호출한다.
# 생성: openssl rand -hex 32 → Vercel env(Production) + Bitwarden. 로컬 개발에선 비워 두면 admin 쿠키로만 실행.
CRON_SECRET=

# KINS 자원 감시 텔레그램 보고 모드 (src/lib/watch/notify.ts). all(기본)=매일 하트비트 · changes=변화·실패만.
WATCH_REPORT=

# 익명 제안 쿼터 HMAC 비밀(src/lib/proposals.ts quotaSecret). 비우면 SUPABASE_SERVICE_ROLE_KEY 의 sha256 파생값을 쓴다.
PROPOSAL_QUOTA_SECRET=

# 관리자 업무 메일 알림 수신자(src/lib/admin-notify.ts). 비우면 PUBLIC_ADMIN_EMAILS 를 쓴다.
# 서버 전용 — PUBLIC_ 과 달리 클라이언트 번들에 노출되지 않는다.
ADMIN_NOTIFY_EMAILS=

# Changelog

형식: [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/). 버전은 `src/consts.ts`(`APP_VERSION`·`APP_RELEASE_DATE`)·`package.json` 과 함께 올린다.
회원 공지는 관리자 **알림 발송** 화면에서 제목 `RadSafety vX.Y.Z 주요 업데이트 안내`, 본문 `RadSafety가 vX.Y.Z로 업데이트되었습니다. 이번 변경은 아래 N가지입니다.` + 번호 목록, **만료 없음** 옵션으로 보낸다(알림함이 업데이트 이력이 된다).

## [Unreleased]

### 사용성 집계 (U 트랙) — U-1 서버 계측

- `usage_events`·`usage_daily`·`usage_active` 신설(`sql_query/migrate_add_usage.sql`). 원시는 35일 후 삭제, 집계본은 영구
- 날짜별 HMAC 익명화(`USAGE_HMAC_SECRET`) — 날이 바뀌면 같은 사람을 이을 수 없다. 주·월까지만 잇고 리텐션·코호트는 의도적으로 포기
- 페이지 조회는 SSR 미들웨어 한 곳에서 기록. 가입·아이디 설정·의견 전송은 서버 액션에서 기록
- **제도 개선 제안 계열 경로는 조회 자체를 기록하지 않는다** — 익명 제출과 계정의 연결이 되살아나지 않게 서버에서 차단
- 허용목록 밖 이벤트·속성은 서버가 버린다. 검색어·본문 등 자유 텍스트는 어떤 경로로도 저장되지 않는다
- 조정 수치 2개를 환경변수로 분리(`USAGE_RETENTION_DAYS` 기본 35 · `USAGE_MIN_DISPLAY_COUNT` 기본 0 = 가리지 않음)
- 상태 점검에 `usage-hmac` 추가 — 값은 반환하지 않고 설정 여부만
- 개인정보처리방침 §3 에 「사용 현황 집계」 문단 추가(현재형). **배포 시 회원 안내 필요**

> 남은 단계: U-2 회원 전용 벽 이탈 퍼널·사건사고·KINS 이벤트 · U-3 클라이언트(오프라인·앱 설치·푸시 허용·로그인 성공) · U-4 야간 롤업과 관리자 화면.

## [1.0.0] - 2026-09-20 — 개인정보 최소화 재설계 + KINS 연계 (정식 1.0)

> 0.x → **1.0**: 로그인 방식 변경(호환성 단절)과 계획한 기능(1·2·3단계·KINS 연계) 완비로 "개발 완료 → 운영"을 선언.
> 개발 중 `0.3.0`·`1.1.0`·`1.1.1`·`1.2.0` 으로 나눠 올렸던 변경은 **회원에게 공지하기 전이라 1.0.0 하나로 합쳤다**(Dr. Ben 2026-09-20) — 아직 알리지 않은 것에 마이너 버전을 나눌 이유가 없다.
> 이후는 시맨틱 버저닝 — 호환 단절 시 2.0, 기능 추가 1.x, 수정 1.0.x.

### 로그인·계정 (1단계)

- 이메일 OTP 로그인 제거 → 아이디/비밀번호·카카오만. 첫 접속 시 아이디 지정 게이트(`/claim-username`)
- 카카오 동의 항목 최소화(이메일만, 닉네임 미요청). 이메일은 계정 생성 직후 파생 주소로 교체
- **비밀번호 변경**(마이페이지) — 아이디 계정은 현재 비밀번호로 재인증 후 변경, 카카오 전용 계정은 최초 설정(아이디 로그인이라는 예비 경로 확보)
- **아이디 변경 기능은 두지 않는다** — 아이디는 작성자 표시의 식별자라 바뀌면 과거 게시물 추적이 끊긴다
- 개인정보처리방침 `/privacy` 신설(시행 2026-09-20)

### 회원·권한 (2단계)

- 회원명부 대조 인증 폐지, `profiles` 개인정보 컬럼 14개·테이블 3개 삭제
- 회원기관 목록 119곳 + '기타' 가입 → 관리자 화면에서 배포 없이 기관 등록(`hospitals_custom`)
- 가입 승인 = 게시 권한. 제출 검토·권한 회수·반려 3회 차단, 비공개 대기 버킷
- 2계층 공개 계층: 비회원은 정기검사 수검준비·사례 목록·자료 다운로드·이용안내 열람

### 제도 개선 제안 (3단계)

- 익명(기본)/아이디 제출, 접수증 코드 조회(`/proposal-lookup`, 공개), 하루 3건 쿼터(HMAC — 제안↔계정 연결 불가)
- 첨부 이미지·PDF 메타데이터 제거 후 비공개 저장(파일당 3MB×3). 관리자 검토·답변·통계 화면
- 회원 전용 유지(가입 = 봇·도배 최소 방어선). 익명 = 로그인 뒤 작성자 미기록
- 아이디 제안 작성자도 **회원 탈퇴 가능** — `proposals` CHECK 의 signed 분기에서 작성자 요구 제거(`migrate_relax_proposal_author_check.sql`). 탈퇴하면 작성자 연결만 끊기고(`(탈퇴한 회원)`) 글은 남는다. **익명 행의 신원 NULL 강제는 불변**

### KINS 연계

- `/kins` 공식 자원 링크(K-2)
- 자원 갱신 감시(K-3): 규제해석 SOS·이용자지원간행물·원안위 보도자료·NSIC 사례집 — 매일 07:00 Vercel Cron, 집합 diff, 텔레그램 하트비트(`WATCH_REPORT`)
- 사건·사고(K-1): `bulletins` 속보→확정본 스레드(자동 제안·관리자 확정), 회원 페이지·홈 배너·오프라인 캐시·정기검사 체크리스트 연결

### 관리자 운영

- **관리자 지정/해제**(회원 목록 `관리자` 열) — 그전까지 SQL 로만 가능했다. 가드: 자기 자신 해제 금지 · 마지막 관리자 해제 금지 · 활성 회원에게만 부여
- **관리자 업무 메일 알림**(`src/lib/admin-notify.ts`) — 새 가입 신청 · 회원기관 등록 요청 · 검토 대기 제출물 · 새 제도개선 제안. 수신자는 `ADMIN_NOTIFY_EMAILS`(서버 전용) 우선, 없으면 기존 `PUBLIC_ADMIN_EMAILS`
- **새 가입 신청 알림 신설** — 그전까지 앱·메일·텔레그램 **어디에도 알림이 없었다**. 이제 앱 알림 + 메일 양쪽
- 메일 본문 원칙: 제목·건수·분류·아이디·링크만. **제안 본문·첨부·작성자는 싣지 않는다**(익명 채널 보호)
- 알림 발송에 "업데이트 안내 — 만료 없음" 옵션(알림함이 업데이트 이력이 된다)

### 보안·운영

- 서버 액션 인증을 **세션 쿠키 기준**으로 전환(클라이언트 `adminId`/`userId` 불신)
- `createAnonClient()` — 비밀번호 재인증 전용 1회성 클라이언트(공유 anon 세션 오염 방지)
- 텔레그램 알림 env 정비, Vercel 비밀값 Secret 잠금, `CRON_SECRET`
- DB 마이그레이션(`sql_query/migrate_*.sql`) 11건, 단위테스트 465·e2e·명세 1:1
- 테스트 계정 5개와 시험 콘텐츠 정리(CI e2e 계정·`monthly-check` 는 유지), `monthly-check` 의 잘못 입력된 소속 비움

## [0.2.1] - 2026-03-20

- 관리자 의견관리 상태(검토중/보류/완료), 사용자 개선의견조회 메뉴, 시작화면 면책 문구, 지적권고사항 메뉴 유지
